---
phase: 02-shared-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/shared/EmptyState.tsx
  - components/dashboard/shared/MetricCard.tsx
  - components/dashboard/shared/SectionHeader.tsx
  - app/dashboard/page.tsx
autonomous: false
requirements: [FOUND-04, FOUND-05, FOUND-06, DSN-01, DSN-06]

must_haves:
  truths:
    - "EmptyState renders all four elements: 48x48 icon container, bold title, description, and optional action button"
    - "MetricCard renders a KPI card with value, label, icon, and optional subtext using the exact CLAUDE.md card anatomy class string"
    - "SectionHeader renders a heading with optional action slot, without wrapping itself in a card"
    - "All three components render correctly in both light and dark mode"
    - "No shadcn Card component is imported anywhere in components/dashboard/shared/"
    - "All icons come from Lucide React exclusively"
  artifacts:
    - path: "components/dashboard/shared/EmptyState.tsx"
      provides: "Shared empty state component for all tabs"
      exports: ["EmptyState"]
      contains: "EmptyStateProps"
    - path: "components/dashboard/shared/MetricCard.tsx"
      provides: "Shared KPI metric card component"
      exports: ["MetricCard"]
      contains: "MetricCardProps"
    - path: "components/dashboard/shared/SectionHeader.tsx"
      provides: "Shared section header component"
      exports: ["SectionHeader"]
      contains: "SectionHeaderProps"
    - path: "app/dashboard/page.tsx"
      provides: "Visual showcase of all three components"
  key_links:
    - from: "components/dashboard/shared/EmptyState.tsx"
      to: "components/ui/button.tsx"
      via: "import Button for optional action"
      pattern: "import.*Button.*from.*components/ui/button"
    - from: "components/dashboard/shared/MetricCard.tsx"
      to: "app/globals.css"
      via: "CSS variable reference in card class string"
      pattern: "var\\(--card-bg\\)|var\\(--card-shadow\\)"
---

<objective>
Build the three shared building-block components (EmptyState, MetricCard, SectionHeader) that every dashboard tab depends on, then update the dashboard page to showcase all three for visual verification in both light and dark mode.

Purpose: These components are imported by all 5 tabs (Phases 4-8). Building and verifying them now prevents downstream drift and duplication.
Output: Three production-ready shared components in components/dashboard/shared/ and a visual showcase on the dashboard page.
</objective>

<execution_context>
@/Users/arnavsinghal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arnavsinghal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-components/02-RESEARCH.md
@.planning/phases/01-foundation-verification/01-01-SUMMARY.md

@app/dashboard/page.tsx
@app/globals.css
@components/ui/button.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From components/ui/button.tsx:
```typescript
// shadcn base-nova Button — uses @base-ui/react, NOT Radix
// Accepts: variant ("default" | "outline" | "secondary" | "ghost" | "destructive" | "link")
// Accepts: size ("default" | "xs" | "sm" | "lg" | "icon" | "icon-xs" | "icon-sm" | "icon-lg")
import { Button } from '@/components/ui/button'
```

From lucide-react:
```typescript
// LucideIcon type for icon props — MUST use this, never `any`
import type { LucideIcon } from 'lucide-react'
```

Card anatomy class string (verbatim from CLAUDE.md, verified in Phase 1):
```
rounded-xl border border-gray-200 dark:border-gray-800 bg-white dark:bg-[var(--card-bg)] [box-shadow:var(--card-shadow)] p-5
```

CRITICAL Tailwind v4 rule: Use `[box-shadow:var(--card-shadow)]` NOT `shadow-[var(--card-shadow)]`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EmptyState, MetricCard, and SectionHeader components</name>
  <files>components/dashboard/shared/EmptyState.tsx, components/dashboard/shared/MetricCard.tsx, components/dashboard/shared/SectionHeader.tsx</files>
  <action>
Create the `components/dashboard/shared/` directory and build all three components following the exact specifications from CLAUDE.md and the research code examples.

**EmptyState.tsx** (FOUND-04):
- `'use client'` directive
- Named `EmptyStateProps` interface with: `icon: LucideIcon`, `title: string`, `description: string`, `actionLabel?: string`, `actionIcon?: LucideIcon`, `onAction?: () => void`
- Centered flex column layout with `py-12 text-center`
- Icon container: `w-12 h-12 rounded-xl bg-gray-100 dark:bg-gray-800` with icon at `w-6 h-6 text-gray-400 dark:text-gray-500`
- Title: `text-sm font-semibold text-gray-900 dark:text-white`
- Description: `text-sm text-gray-500 dark:text-gray-400 max-w-xs mt-1`
- Optional action button: shadcn `Button` with `variant="outline" size="sm"`, rendered only when both `actionLabel` and `onAction` are provided. If `actionIcon` is provided, render it at `w-4 h-4 mr-1.5` inside the button.
- Import Button from `@/components/ui/button`

**MetricCard.tsx** (FOUND-05, DSN-01):
- `'use client'` directive
- Named `MetricCardProps` interface with: `label: string`, `value: string | number`, `icon: LucideIcon`, `subtext?: string`, `iconClassName?: string`
- Wraps content in a card div using the EXACT CLAUDE.md card anatomy class string: `rounded-xl border border-gray-200 dark:border-gray-800 bg-white dark:bg-[var(--card-bg)] [box-shadow:var(--card-shadow)] p-5`
- Layout: flex row with label left and icon right, value below, optional subtext below value
- Label: `text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wide`
- Value: `text-3xl font-bold text-gray-900 dark:text-white leading-none mt-2`
- Icon: default `w-5 h-5 text-gray-400 dark:text-gray-500`, overridable via `iconClassName` prop
- Subtext: `text-xs text-gray-400 dark:text-gray-500 mt-1`
- NEVER import shadcn Card. Use raw `<div>`.

**SectionHeader.tsx** (FOUND-06):
- `'use client'` directive
- Named `SectionHeaderProps` interface with: `title: string`, `subtitle?: string`, `action?: ReactNode` (import ReactNode from 'react')
- Flex row: title/subtitle left, action right, `mb-4` bottom margin
- Title: `text-base font-semibold text-gray-900 dark:text-white`
- Subtitle: `text-sm text-gray-500 dark:text-gray-400 leading-relaxed`
- This component does NOT render a card wrapper. It is a header row that goes INSIDE a card.

All three components: export as named exports (not default). Use `LucideIcon` type from `lucide-react` for icon props (DSN-06). No `any` types anywhere.
  </action>
  <verify>
    <automated>cd /Users/arnavsinghal/DashboardSidekick/sidekick-dashboard && npx next build 2>&1 | tail -5 && grep -r "from.*components/ui/card" components/dashboard/shared/ ; echo "exit: $?"</automated>
  </verify>
  <done>All three component files exist in components/dashboard/shared/, build passes with zero TypeScript errors, and no shadcn Card import is found in any shared component file.</done>
</task>

<task type="auto">
  <name>Task 2: Update dashboard page to showcase all three shared components</name>
  <files>app/dashboard/page.tsx</files>
  <action>
Replace the Phase 1 verification stub in `app/dashboard/page.tsx` with a showcase that renders all three shared components so they can be visually verified in both light and dark mode.

Keep the existing: `'use client'`, `useState`/`useEffect` imports, `useTheme` hook, mounted guard pattern, theme toggle button, `min-h-screen bg-[var(--page-bg)]` wrapper, `max-w-7xl mx-auto px-6 py-6` content area.

Add below the theme toggle:

1. **SectionHeader showcase** — Inside a card div (using the exact CLAUDE.md card class string), render a SectionHeader with title "Team Overview" and an action slot containing a ghost Button with a RefreshCw icon (from lucide-react).

2. **MetricCard grid** — A `grid grid-cols-4 gap-4` row (responsive: `md:grid-cols-2 sm:grid-cols-1`) with 4 MetricCards:
   - "Questions Today" / value 42 / MessageSquare icon / subtext "12 this week"
   - "Answer Accuracy" / value "94%" / Target icon / subtext "Up from 89%"
   - "Open Issues" / value 3 / AlertTriangle icon / iconClassName "w-5 h-5 text-amber-500" / subtext "View all"
   - "Health Score" / value 87 / Activity icon / iconClassName "w-5 h-5 text-green-500" / subtext "Good"

3. **EmptyState showcase** — Inside a card div (exact CLAUDE.md class string), render an EmptyState with:
   - icon: FileText (from lucide-react)
   - title: "No documents uploaded"
   - description: "Upload training documents to build your team's AI knowledge base. Supported formats include PDF, Word, and Excel."
   - actionLabel: "Upload Document"
   - actionIcon: Upload (from lucide-react)
   - onAction: `() => {}` (no-op for now)

Import all needed Lucide icons at the top: `MessageSquare, Target, AlertTriangle, Activity, FileText, Upload, RefreshCw, Sun, Moon`.
  </action>
  <verify>
    <automated>cd /Users/arnavsinghal/DashboardSidekick/sidekick-dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Dashboard page renders all three shared components in a showcase layout. Build passes. The page displays: a SectionHeader inside a card, 4 MetricCards in a grid, and an EmptyState inside a card — all ready for visual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of shared components in both modes</name>
  <action>
Human verifies all three shared components render correctly in light and dark mode on the dashboard showcase page.
  </action>
  <what-built>Three shared components (EmptyState, MetricCard, SectionHeader) showcased on the dashboard page with mock data, theme toggle, and full dark mode support.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000 (redirects to /dashboard)
    2. Verify LIGHT MODE:
       - Page background is light gray (#f9fafb)
       - SectionHeader shows "Team Overview" heading with a refresh icon button on the right, inside a white card with shadow
       - 4 MetricCards in a row, each a white card with shadow, showing: value (large bold), label (small caps above), icon (top right), subtext (small gray below value)
       - "Open Issues" card icon is amber, "Health Score" icon is green
       - EmptyState inside a card shows: gray rounded icon container with FileText icon, bold "No documents uploaded" title, gray description text, outline "Upload Document" button with upload icon
    3. Toggle to DARK MODE (click moon/sun icon):
       - Page background is dark (#0f1117)
       - All cards have dark surface (#1a1d27) with visible shadow
       - All text is readable — white headings, gray-400 secondary text
       - EmptyState icon container is dark gray (bg-gray-800)
    4. Verify NO shadcn Card artifacts — cards should be clean divs with subtle shadow, not the shadcn Card styled wrapper
  </how-to-verify>
  <done>Human confirms all three components render correctly in both light and dark mode.</done>
  <resume-signal>Type "approved" or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
Phase-level verification after all tasks complete:

1. Build passes: `npx next build` exits 0
2. Static analysis — no banned imports:
   - `grep -r "from.*components/ui/card" components/dashboard/shared/` returns empty
   - `grep -r "from.*heroicons\|from.*feather" components/dashboard/shared/` returns empty
3. All three files exist: `ls components/dashboard/shared/{EmptyState,MetricCard,SectionHeader}.tsx`
4. Each file has named Props interface: `grep "Props" components/dashboard/shared/*.tsx` shows EmptyStateProps, MetricCardProps, SectionHeaderProps
5. Visual verification in browser confirms both light and dark mode rendering
</verification>

<success_criteria>
- EmptyState renders icon container (48x48 rounded-xl) + bold title + description + optional outline action button
- MetricCard renders inside a card div with exact CLAUDE.md class string — large bold value, small caps label, icon, optional subtext
- SectionHeader renders as a flex row (not a card) with heading and optional right-aligned action
- All three are visually correct in both light and dark mode
- No shadcn Card import exists in components/dashboard/shared/
- No non-Lucide icon imports exist
- Build passes with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-components/02-01-SUMMARY.md`
</output>
