---
phase: 03-layout-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/layout/TopBar.tsx
  - components/dashboard/layout/SubHeader.tsx
autonomous: true
requirements:
  - FOUND-02
  - DARK-03

must_haves:
  truths:
    - "TopBar renders logo (MessageCircle + Sidekick | Dashboard), home icon, 3-state theme toggle, and avatar dropdown"
    - "Theme toggle cycles light -> dark -> system with correct icon (Moon -> Sun -> Monitor)"
    - "Avatar dropdown opens with user name, Settings, and Sign Out items"
    - "SubHeader renders static tagline and company selector with 3 mock companies"
    - "TopBar and SubHeader share card background, forming one elevated header block"
    - "Both components render correctly in light and dark mode"
  artifacts:
    - path: "components/dashboard/layout/TopBar.tsx"
      provides: "Dashboard top bar with logo, theme toggle, home icon, avatar dropdown"
      exports: ["TopBar", "TopBarProps"]
    - path: "components/dashboard/layout/SubHeader.tsx"
      provides: "Dashboard sub-header with tagline and company selector"
      exports: ["SubHeader", "SubHeaderProps"]
  key_links:
    - from: "components/dashboard/layout/TopBar.tsx"
      to: "next-themes"
      via: "useTheme() with mounted guard"
      pattern: "useTheme.*setTheme"
    - from: "components/dashboard/layout/TopBar.tsx"
      to: "components/ui/dropdown-menu.tsx"
      via: "Avatar dropdown menu"
      pattern: "DropdownMenu"
    - from: "components/dashboard/layout/SubHeader.tsx"
      to: "components/ui/select.tsx"
      via: "Company selector"
      pattern: "Select"
---

<objective>
Build TopBar and SubHeader layout components for the dashboard chrome.

Purpose: These form the top elevated header block — the first things a manager sees. TopBar provides branding, navigation, and the critical theme toggle. SubHeader provides context (tagline) and multi-location switching (company selector).

Output: Two production-ready layout components with full dark mode support, ready to be composed in page.tsx.
</objective>

<execution_context>
@/Users/arnavsinghal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arnavsinghal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-layout-shell/03-CONTEXT.md
@.planning/phases/03-layout-shell/03-RESEARCH.md

@app/dashboard/page.tsx
@components/dashboard/shared/EmptyState.tsx

<interfaces>
<!-- Existing shadcn components the executor will use -->

From components/ui/button.tsx:
- Button component with variants: default, destructive, outline, secondary, ghost, link
- Sizes: default, sm, lg, icon

From components/ui/dropdown-menu.tsx:
- DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuLabel, DropdownMenuSeparator, DropdownMenuItem

From components/ui/select.tsx:
- Select, SelectTrigger, SelectValue, SelectContent, SelectItem

From components/ui/avatar.tsx:
- Avatar, AvatarImage, AvatarFallback

CRITICAL base-nova note: Use `render={}` prop instead of `asChild` for custom trigger elements.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build TopBar component</name>
  <files>components/dashboard/layout/TopBar.tsx</files>
  <action>
Create `components/dashboard/layout/TopBar.tsx` with named export `TopBar` and `TopBarProps` interface.

**Layout:** Full-width bar with `bg-white dark:bg-[var(--card-bg)]` background (NO border-bottom — SubHeader shares the same elevated block). Inner content: `max-w-7xl mx-auto px-6 flex items-center justify-between h-14`.

**Left side — Logo:**
- MessageCircle icon (h-5 w-5, text-blue-600 dark:text-blue-400)
- "Sidekick" text (text-base font-semibold text-gray-900 dark:text-white)
- "|" separator (text-sm text-gray-400 dark:text-gray-500)
- "Dashboard" text (text-sm text-gray-500 dark:text-gray-400)

**Right side — Actions (left to right: Home, Theme Toggle, Avatar):**
- Home icon: Ghost Button (size="icon", 32x32), Home icon from Lucide (h-4 w-4). Does nothing (placeholder).
- Theme toggle: Ghost Button (size="icon", 32x32). Uses `useTheme()` with `mounted` guard pattern. Cycles: light (shows Moon icon) -> dark (shows Sun icon) -> system (shows Monitor icon). The `cycleTheme` function: if light -> setTheme('dark'), if dark -> setTheme('system'), else -> setTheme('light'). Only render when `mounted` is true. Add `aria-label="Toggle theme"`.
- Avatar: DropdownMenu wrapping an Avatar with AvatarFallback "N" (purple bg-purple-600 text-white). The trigger should be a ghost button with rounded-full. Dropdown content (align="end", sideOffset={8}):
  - DropdownMenuLabel: "Nate Manager" (mock name)
  - DropdownMenuSeparator
  - DropdownMenuItem: "Settings"
  - DropdownMenuSeparator
  - DropdownMenuItem: "Sign Out"

**Patterns to follow:**
- `'use client'` directive at top
- Named export (not default)
- Named `TopBarProps` interface
- `mounted` guard: `useState(false)` + `useEffect(() => setMounted(true), [])`
- Render theme-dependent icon only when mounted

**AVOID:** Do NOT add border-bottom to TopBar. Do NOT use `asChild` (use `render={}` if needed for DropdownMenuTrigger). Do NOT use any icon library other than lucide-react.
  </action>
  <verify>
    <automated>cd /Users/arnavsinghal/DashboardSidekick/sidekick-dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>TopBar.tsx exists with named exports, renders logo + home + 3-state theme toggle + avatar dropdown, uses mounted guard, builds without errors</done>
</task>

<task type="auto">
  <name>Task 2: Build SubHeader component</name>
  <files>components/dashboard/layout/SubHeader.tsx</files>
  <action>
Create `components/dashboard/layout/SubHeader.tsx` with named export `SubHeader` and `SubHeaderProps` interface.

**Layout:** Full-width bar with `bg-white dark:bg-[var(--card-bg)]` background (same as TopBar — one elevated block). Inner content: `max-w-7xl mx-auto px-6 flex items-center justify-between py-3`.

**Left side — Tagline:**
- Static text: "An overview of how your team is using Sidekick."
- Typography: `text-sm text-gray-500 dark:text-gray-400`

**Right side — Company Selector:**
- Use shadcn Select component with `defaultValue="sunrise-cafe"`
- SelectTrigger: styled compact with Building icon (h-4 w-4 text-gray-500) before SelectValue. Trigger should have a subtle border and reasonable width (w-[200px] or similar). Style: `border-gray-200 dark:border-gray-700 bg-transparent`
- SelectContent with 3 SelectItems:
  - value="sunrise-cafe" label="Sunrise Cafe"
  - value="metro-warehouse" label="Metro Warehouse"
  - value="downtown-retail" label="Downtown Retail"
- Company selector interactivity: Use controlled state (useState) so selecting a different company updates the displayed name. This makes the demo feel responsive.

**CRITICAL:** No border-bottom on SubHeader either. The border comes from TabNav's container in Plan 02. TopBar + SubHeader = one seamless elevated block.

**Patterns to follow:**
- `'use client'` directive
- Named export + `SubHeaderProps` interface
- Import Building from lucide-react
- Follow the established pattern from shared components (named exports, typed props)

**AVOID:** Do NOT add border between TopBar and SubHeader. Do NOT change the tagline per tab — it's static.
  </action>
  <verify>
    <automated>cd /Users/arnavsinghal/DashboardSidekick/sidekick-dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>SubHeader.tsx exists with named exports, renders static tagline and working company selector with 3 options, shares card background with TopBar, builds without errors</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds (TypeScript + compilation check)
- Both files exist in `components/dashboard/layout/`
- Both use named exports and named Props interfaces
- TopBar theme toggle uses mounted guard pattern
- Neither component has border-bottom (they form one seamless block)
- Both use `bg-white dark:bg-[var(--card-bg)]` background
</verification>

<success_criteria>
- TopBar renders with logo, home icon, 3-state theme toggle (light/dark/system), and avatar dropdown
- SubHeader renders with static tagline and working company selector (3 mock companies)
- Both components share card background, forming one elevated header block
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-layout-shell/03-01-SUMMARY.md`
</output>
