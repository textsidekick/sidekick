---
phase: 07-documents-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/documents/UploadZone.tsx
  - components/dashboard/documents/IntegrationsRow.tsx
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DSN-02
  - DSN-04

must_haves:
  truths:
    - "UploadZone renders a dashed-border drop area with upload icon, title text, and helper text in both light and dark mode"
    - "UploadZone shows visual hover/drag feedback (border color change and background tint)"
    - "IntegrationsRow renders 4 integration cards in a 2x2 grid with Lucide icon placeholders"
    - "All 4 integration connect buttons use variant='outline' with neutral border — zero brand-colored fills"
  artifacts:
    - path: "components/dashboard/documents/UploadZone.tsx"
      provides: "Drag-and-drop file upload zone with visual feedback"
      exports: ["UploadZone"]
    - path: "components/dashboard/documents/IntegrationsRow.tsx"
      provides: "4 integration connector cards with neutral outline buttons"
      exports: ["IntegrationsRow"]
  key_links:
    - from: "UploadZone.tsx"
      to: "CLAUDE.md upload zone spec"
      via: "exact class string match"
      pattern: "border-2 border-dashed"
    - from: "IntegrationsRow.tsx"
      to: "Button component"
      via: "variant='outline' prop"
      pattern: "variant=\"outline\""
---

<objective>
Build the UploadZone and IntegrationsRow components for the Documents tab.

Purpose: These are the two novel components in the Documents tab — UploadZone is a styled drag-and-drop area with visual feedback, and IntegrationsRow is a 2x2 grid of integration connector cards. Both must strictly follow CLAUDE.md design rules (dashed border spec for upload, neutral outline buttons for integrations).

Output: Two production-ready components in components/dashboard/documents/ ready for assembly in Plan 02.
</objective>

<execution_context>
@/Users/arnavsinghal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arnavsinghal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-documents-tab/07-RESEARCH.md

<interfaces>
<!-- Existing components this plan's outputs will be consumed by (Plan 02 DocumentsTab assembly) -->

From components/ui/button.tsx:
```typescript
// shadcn Button — use variant="outline" for all integration connect buttons
<Button variant="outline">Connect</Button>
```

From components/dashboard/shared/SectionHeader.tsx:
```typescript
interface SectionHeaderProps {
  title: string
  action?: React.ReactNode
}
export function SectionHeader({ title, action }: SectionHeaderProps)
```

CLAUDE.md Upload Zone class string:
```
border-2 border-dashed border-gray-200 dark:border-gray-700 rounded-xl
bg-white dark:bg-[var(--card-bg)]
hover:border-blue-300 hover:bg-blue-50/30 dark:hover:border-blue-700
transition-colors duration-150 cursor-pointer
min-h-[160px] flex flex-col items-center justify-center gap-3
```

CLAUDE.md Card anatomy class string:
```
rounded-xl border border-gray-200 dark:border-gray-800
bg-white dark:bg-[var(--card-bg)]
[box-shadow:var(--card-shadow)]
p-5
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build UploadZone component</name>
  <files>components/dashboard/documents/UploadZone.tsx</files>
  <action>
Create `components/dashboard/documents/UploadZone.tsx` with:

1. `'use client'` directive at top.
2. Named `UploadZoneProps` interface (empty for now — no props needed in v1).
3. Named export `UploadZone`.
4. `isDragOver` state (useState boolean, default false) for visual drag feedback.
5. A hidden `<input type="file" ref={fileInputRef} accept=".pdf,.doc,.docx,.xls,.xlsx,.txt" multiple className="hidden" />` — triggered by onClick on the drop area. No `<form>` tag.
6. The drop area div uses the EXACT CLAUDE.md upload zone class string. When `isDragOver` is true, apply active state classes: `border-blue-400 bg-blue-50/30 dark:border-blue-600` (replacing the default border/hover classes). Use `cn()` from `@/lib/utils` for conditional class merging.
7. Inside the drop area:
   - `Upload` icon from lucide-react at `h-8 w-8 text-gray-400 dark:text-gray-500`
   - Primary text: `"Drop files here or click to upload"` using `text-sm font-medium text-gray-700 dark:text-gray-300`
   - Helper text: `"PDF, Word, Excel, or text files"` using `text-xs text-gray-400 dark:text-gray-500 mt-1`
8. Event handlers: `onDragOver` (preventDefault + setIsDragOver true), `onDragLeave` (setIsDragOver false), `onDrop` (preventDefault + setIsDragOver false — no file processing in v1), `onClick` (trigger hidden file input).
9. This is a FULL-WIDTH component — do NOT add max-w-2xl. DSN-04 constraint applies only to AI Studio VideoUpload (Phase 8), not Documents UploadZone per research findings and screenshot observations.
  </action>
  <verify>
    <automated>cd /Users/arnavsinghal/DashboardSidekick/sidekick-dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>UploadZone.tsx exists, exports UploadZone, builds without errors, uses exact CLAUDE.md upload zone classes, has drag visual feedback, no form tag, no max-w-2xl constraint.</done>
</task>

<task type="auto">
  <name>Task 2: Build IntegrationsRow component</name>
  <files>components/dashboard/documents/IntegrationsRow.tsx</files>
  <action>
Create `components/dashboard/documents/IntegrationsRow.tsx` with:

1. `'use client'` directive at top.
2. Named `IntegrationsRowProps` interface (empty for now).
3. Named export `IntegrationsRow`.
4. Define `IntegrationItem` interface: `{ id: string; name: string; description: string; icon: LucideIcon; connected: boolean }`.
5. Define `INTEGRATIONS` constant array with 4 items:
   - Google Drive: icon=`HardDrive`, description="Import documents from Google Drive"
   - Dropbox: icon=`Cloud`, description="Import documents from Dropbox"
   - Gusto: icon=`Building2`, description="Import documents from Gusto"
   - Microsoft 365: icon=`Monitor`, description="Import documents from Microsoft 365"
   All with `connected: false`.
6. Import `LucideIcon` type from lucide-react for the icon prop typing.
7. Render a `SectionHeader` with title "Import from Integrations" (no action).
8. Below it, a `grid grid-cols-1 sm:grid-cols-2 gap-4 mt-4` containing 4 integration cards.
9. Each integration card is a raw `<div>` with the CLAUDE.md card anatomy class string: `rounded-xl border border-gray-200 dark:border-gray-800 bg-white dark:bg-[var(--card-bg)] [box-shadow:var(--card-shadow)] p-5`.
10. Inside each card: a `flex items-center justify-between` layout with:
    - Left side: `flex items-center gap-3` containing a 40x40 rounded-lg icon container (`bg-gray-100 dark:bg-gray-800 flex items-center justify-center`) with the Lucide icon at `h-5 w-5 text-gray-600 dark:text-gray-400`, then a div with the service name (`text-sm font-semibold text-gray-900 dark:text-white`) and description (`text-xs text-gray-500 dark:text-gray-400`).
    - Right side: `<Button variant="outline" size="sm">Connect</Button>` — NEUTRAL styling only. NEVER add brand colors. This is the single most important visual rule per CLAUDE.md.
11. Wrap the entire section (SectionHeader + grid) in a container div (no card wrapper — the individual integration cards ARE the cards).

CRITICAL: Every connect button MUST be `variant="outline"`. If you find yourself writing `bg-blue-600`, `bg-orange-500`, or `bg-purple-600` on any connect button, STOP. The Lucide icon placeholder provides service recognition. The button stays calm and neutral.
  </action>
  <verify>
    <automated>cd /Users/arnavsinghal/DashboardSidekick/sidekick-dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>IntegrationsRow.tsx exists, exports IntegrationsRow, builds without errors. All 4 connect buttons use variant="outline" with no brand color overrides. Icons are Lucide only. Cards use raw divs with exact CLAUDE.md class string. 2x2 grid layout on sm+ breakpoint.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds with no TypeScript errors
- No import of shadcn Card component in either file
- No `<form>` tags in UploadZone
- No brand-colored fills (bg-blue-600, bg-orange-500, bg-purple-600) on any integration button
- No max-w-2xl on UploadZone (that constraint is Phase 8 AI Studio only)
- Both files use 'use client', named Props interfaces, named exports
</verification>

<success_criteria>
- UploadZone renders full-width dashed-border drop area with drag visual feedback
- IntegrationsRow renders 2x2 grid of 4 integration cards with neutral outline connect buttons
- Both components build cleanly and follow all CLAUDE.md design system rules
</success_criteria>

<output>
After completion, create `.planning/phases/07-documents-tab/07-01-SUMMARY.md`
</output>
