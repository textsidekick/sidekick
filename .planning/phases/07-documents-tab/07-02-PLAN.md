---
phase: 07-documents-tab
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - components/dashboard/documents/DocumentsTable.tsx
  - components/dashboard/documents/DocumentsTab.tsx
  - app/dashboard/page.tsx
autonomous: true
requirements:
  - DOCS-03
  - DOCS-04

must_haves:
  truths:
    - "DocumentsTable renders columns for name, type, size, and upload date with mock document data"
    - "DocumentsTable shows EmptyState when documents array is empty"
    - "DocumentsTab accepts showMockData prop — true populates all sections, false shows empty state in table"
    - "Selecting the Documents tab in the dashboard shows the full Documents tab (not the Coming Soon placeholder)"
  artifacts:
    - path: "components/dashboard/documents/DocumentsTable.tsx"
      provides: "Document list table with empty state"
      exports: ["DocumentsTable", "MOCK_DOCUMENTS"]
    - path: "components/dashboard/documents/DocumentsTab.tsx"
      provides: "Assembly component composing UploadZone + IntegrationsRow + DocumentsTable"
      exports: ["DocumentsTab"]
    - path: "app/dashboard/page.tsx"
      provides: "Dashboard page with DocumentsTab replacing Coming Soon placeholder"
  key_links:
    - from: "DocumentsTab.tsx"
      to: "UploadZone.tsx"
      via: "import and render"
      pattern: "import.*UploadZone"
    - from: "DocumentsTab.tsx"
      to: "IntegrationsRow.tsx"
      via: "import and render"
      pattern: "import.*IntegrationsRow"
    - from: "DocumentsTab.tsx"
      to: "DocumentsTable.tsx"
      via: "import, pass documents array"
      pattern: "import.*DocumentsTable"
    - from: "app/dashboard/page.tsx"
      to: "DocumentsTab.tsx"
      via: "conditional render on activeTab"
      pattern: "activeTab === 'documents'"
---

<objective>
Build the DocumentsTable, assemble the DocumentsTab, and wire it into the dashboard page.

Purpose: Complete the Documents tab by adding the documents table (following the established WorkersTable/AlertsTable pattern), composing all three sub-components into DocumentsTab, and replacing the Coming Soon placeholder in page.tsx.

Output: Fully functional Documents tab visible in the dashboard with mock data.
</objective>

<execution_context>
@/Users/arnavsinghal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arnavsinghal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-documents-tab/07-RESEARCH.md
@.planning/phases/07-documents-tab/07-01-SUMMARY.md

<interfaces>
<!-- Components from Plan 01 that this plan consumes -->

From components/dashboard/documents/UploadZone.tsx (created in Plan 01):
```typescript
export function UploadZone(): JSX.Element
// No props — self-contained drag-and-drop area
```

From components/dashboard/documents/IntegrationsRow.tsx (created in Plan 01):
```typescript
export function IntegrationsRow(): JSX.Element
// No props — self-contained 2x2 integration grid
```

<!-- Existing shared components -->

From components/dashboard/shared/SectionHeader.tsx:
```typescript
interface SectionHeaderProps { title: string; action?: React.ReactNode }
export function SectionHeader({ title, action }: SectionHeaderProps)
```

From components/dashboard/shared/EmptyState.tsx:
```typescript
interface EmptyStateProps { icon: LucideIcon; title: string; description: string; action?: React.ReactNode }
export function EmptyState({ icon, title, description, action }: EmptyStateProps)
```

<!-- Pattern reference: WorkersTable structure -->
From components/dashboard/workers/WorkersTable.tsx:
```typescript
// Card wrapper div with CLAUDE.md class string
// SectionHeader with count title + action slot
// Conditional: EmptyState when empty, Table when populated
// MOCK_WORKERS exported alongside component
```

<!-- Pattern reference: WorkersTab assembly -->
From components/dashboard/workers/WorkersTab.tsx:
```typescript
interface WorkersTabProps { showMockData?: boolean }
function WorkersTab({ showMockData = false }: WorkersTabProps) {
  const workers = showMockData ? MOCK_WORKERS : []
  return <div className="space-y-6">...</div>
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build DocumentsTable with mock data</name>
  <files>components/dashboard/documents/DocumentsTable.tsx</files>
  <action>
Create `components/dashboard/documents/DocumentsTable.tsx` following the exact WorkersTable pattern:

1. `'use client'` directive at top.
2. Define `DocumentItem` interface: `{ id: string; name: string; type: string; size: string; uploadDate: string }`.
3. Define `MOCK_DOCUMENTS` constant array with 6 items (from research):
   - 'Employee Safety Manual', PDF, 2.4 MB, 2026-03-01
   - 'Equipment Checklist', Word, 156 KB, 2026-03-05
   - 'Shift Schedule Template', Excel, 89 KB, 2026-03-08
   - 'Onboarding Guide', PDF, 1.8 MB, 2026-03-10
   - 'Cleaning Procedures', PDF, 3.1 MB, 2026-03-12
   - 'Emergency Contacts', Text, 12 KB, 2026-03-15
4. Define `DocumentsTableProps` interface: `{ documents: DocumentItem[] }`.
5. Named export `DocumentsTable` function component.
6. Outer wrapper: raw div with exact CLAUDE.md card anatomy class string.
7. `SectionHeader` with title `"${documents.length} Documents"` and action slot containing a ghost icon button with `RefreshCw` icon at h-4 w-4 (matching WorkersTable/AlertsTable pattern).
8. Conditional render:
   - Empty: `EmptyState` with `FileText` icon, title "No documents uploaded", description "Upload training documents, safety manuals, or policies. They'll appear here once added."
   - Populated: shadcn `Table` with columns: Name, Type, Size, Uploaded.
9. Table header cells use: `text-xs font-medium uppercase tracking-wide text-gray-500 dark:text-gray-400` (exact CLAUDE.md typography).
10. Table body cells:
    - Name: `text-sm font-medium text-gray-900 dark:text-white` (primary cell)
    - Type: `Badge variant="secondary"` with subtle color coding — PDF: `bg-red-100 text-red-700 dark:bg-red-950 dark:text-red-400`, Word: `bg-blue-100 text-blue-700 dark:bg-blue-950 dark:text-blue-400`, Excel: `bg-green-100 text-green-700 dark:bg-green-950 dark:text-green-400`, Text: `bg-gray-100 text-gray-600 dark:bg-gray-800 dark:text-gray-400`. All with `border-transparent`.
    - Size: `text-sm text-gray-500 dark:text-gray-400` (secondary cell)
    - Uploaded: `text-sm text-gray-500 dark:text-gray-400` — use same `formatDate` helper as WorkersTable (toLocaleDateString with month short, day numeric, year numeric), with the noon-time fix to avoid timezone offset issues.
11. Table row styles: `border-b border-gray-100 hover:bg-gray-50 dark:border-gray-800 dark:hover:bg-gray-800/50` (matching WorkersTable).
12. Export `MOCK_DOCUMENTS` alongside the component for use by DocumentsTab.
  </action>
  <verify>
    <automated>cd /Users/arnavsinghal/DashboardSidekick/sidekick-dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>DocumentsTable.tsx exists with 6 mock documents, renders table with Name/Type/Size/Uploaded columns, shows EmptyState when empty, uses exact CLAUDE.md card and typography classes, exports DocumentsTable and MOCK_DOCUMENTS.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble DocumentsTab and wire into dashboard page</name>
  <files>components/dashboard/documents/DocumentsTab.tsx, app/dashboard/page.tsx</files>
  <action>
**DocumentsTab.tsx** — Create following the exact WorkersTab pattern:

1. `'use client'` directive.
2. Import `UploadZone` from `./UploadZone`, `IntegrationsRow` from `./IntegrationsRow`, `DocumentsTable` and `MOCK_DOCUMENTS` from `./DocumentsTable`.
3. Define `DocumentsTabProps` interface: `{ showMockData?: boolean }`.
4. Named export `DocumentsTab`.
5. Body: `const documents = showMockData ? MOCK_DOCUMENTS : []`
6. Return `<div className="space-y-6">` containing:
   - `<UploadZone />`
   - `<IntegrationsRow />`
   - `<DocumentsTable documents={documents} />`

**app/dashboard/page.tsx** — Update to replace Documents Coming Soon placeholder:

1. Add import: `import { DocumentsTab } from '@/components/dashboard/documents/DocumentsTab'`
2. Replace the `{activeTab === 'documents' && (<EmptyState ... />)}` block with: `{activeTab === 'documents' && <DocumentsTab showMockData={true} />}`
3. Remove the `FileText` import from lucide-react ONLY if it is no longer used by any other part of the page (check — it may still be needed). If only the Documents placeholder used it, remove it.
  </action>
  <verify>
    <automated>cd /Users/arnavsinghal/DashboardSidekick/sidekick-dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>DocumentsTab.tsx assembles UploadZone + IntegrationsRow + DocumentsTable with showMockData prop. Dashboard page renders DocumentsTab instead of Coming Soon placeholder. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds with no TypeScript errors
- DocumentsTable follows WorkersTable pattern (card wrapper, SectionHeader, conditional EmptyState/Table)
- DocumentsTab follows WorkersTab pattern (showMockData prop, space-y-6 layout)
- page.tsx imports DocumentsTab and renders it for the documents tab
- No shadcn Card imports, no form tags, no brand-colored buttons
- File type badges use subtle color coding similar to AlertsTable severity badges
</verification>

<success_criteria>
- Documents tab is fully functional in the dashboard
- showMockData={true} shows upload zone, 4 integration cards, and 6 documents in table
- showMockData={false} shows upload zone, 4 integration cards, and EmptyState in table area
- All components render correctly in both light and dark mode
</success_criteria>

<output>
After completion, create `.planning/phases/07-documents-tab/07-02-SUMMARY.md`
</output>
