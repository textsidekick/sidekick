---
phase: 08-ai-studio-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/ai-studio/VideoUpload.tsx
  - components/dashboard/ai-studio/KnowledgeGaps.tsx
autonomous: true
requirements: [AIST-01, AIST-02]

must_haves:
  truths:
    - "VideoUpload renders a dashed-border drag-and-drop zone constrained to max-w-2xl"
    - "VideoUpload has a purple icon container, heading, subtitle, and tips section"
    - "KnowledgeGaps renders an amber Analyze Gaps button (not blue)"
    - "KnowledgeGaps renders gap items when data is provided, EmptyState when empty"
    - "Both components render correctly in light and dark mode"
  artifacts:
    - path: "components/dashboard/ai-studio/VideoUpload.tsx"
      provides: "Video walkthrough upload zone with max-w-2xl constraint"
      exports: ["VideoUpload", "VideoUploadProps"]
    - path: "components/dashboard/ai-studio/KnowledgeGaps.tsx"
      provides: "Knowledge gaps section with amber CTA and conditional empty state"
      exports: ["KnowledgeGaps", "KnowledgeGapsProps", "KnowledgeGap", "MOCK_GAPS"]
  key_links:
    - from: "components/dashboard/ai-studio/KnowledgeGaps.tsx"
      to: "components/dashboard/shared/EmptyState.tsx"
      via: "import EmptyState"
      pattern: "import.*EmptyState"
    - from: "components/dashboard/ai-studio/KnowledgeGaps.tsx"
      to: "components/ui/button"
      via: "import Button for amber CTA"
      pattern: "bg-amber-500"
---

<objective>
Build the two AI Studio sub-components: VideoUpload (drag-and-drop video zone with max-w-2xl) and KnowledgeGaps (gap analysis section with amber Analyze Gaps CTA).

Purpose: These are the building blocks for the AI Studio tab assembly in Plan 02.
Output: Two new component files in `components/dashboard/ai-studio/`.
</objective>

<execution_context>
@/Users/arnavsinghal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arnavsinghal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ai-studio-tab/08-RESEARCH.md

<interfaces>
<!-- Existing components the executor needs -->

From components/dashboard/documents/UploadZone.tsx (template for VideoUpload):
```typescript
// Drag-and-drop pattern: isDragOver state, hidden file input, cn() conditional classes
// onClick triggers fileInputRef.current?.click()
// onDragOver/onDragLeave/onDrop manage isDragOver state
```

From components/dashboard/shared/EmptyState.tsx:
```typescript
interface EmptyStateProps {
  icon: LucideIcon
  title: string
  description: string
  actionLabel?: string
  actionIcon?: LucideIcon
  onAction?: () => void
}
export { EmptyState }
```

From components/dashboard/shared/SectionHeader.tsx:
```typescript
interface SectionHeaderProps {
  title: string
  action?: ReactNode
}
export { SectionHeader }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build VideoUpload component</name>
  <files>components/dashboard/ai-studio/VideoUpload.tsx</files>
  <action>
Create `components/dashboard/ai-studio/VideoUpload.tsx` following the UploadZone pattern from Documents tab.

Structure (top to bottom inside a card wrapper div):
1. **Icon container + heading row:** Purple icon container (48x48, `rounded-xl bg-purple-100 dark:bg-purple-900/30`) with `Video` icon (lucide-react, `h-6 w-6 text-purple-600 dark:text-purple-400`). Next to it: heading "Video Walkthrough" (`text-base font-semibold text-gray-900 dark:text-white`) and subtitle "Upload a video walkthrough of your facility to train the AI" (`text-sm text-gray-500 dark:text-gray-400 leading-relaxed`).

2. **Upload zone (MUST be inside `max-w-2xl` div):** Clone the exact drag-and-drop pattern from `UploadZone.tsx`:
   - Hidden `<input type="file" accept=".mp4,.mov">` (no `multiple`, single video)
   - `isDragOver` state with `cn()` conditional classes
   - Same dashed-border class string from CLAUDE.md upload zone spec
   - Upload icon: `Upload` from lucide-react (`h-8 w-8 text-gray-400`)
   - Primary text: "Upload facility walkthrough video" (`text-sm font-medium text-gray-700 dark:text-gray-300`)
   - Secondary text: "MP4, MOV up to 100MB - 1-5 minutes recommended" (`text-xs text-gray-400 dark:text-gray-500 mt-1`)

3. **Tips section (also inside `max-w-2xl`):** Below the upload zone, `mt-4`:
   - Header: lightbulb emoji + "Tips for best results:" (`text-sm font-medium text-gray-700 dark:text-gray-300`)
   - 4 bullet points as a `<ul>` with `list-disc list-inside space-y-1 mt-2` and `text-sm text-gray-500 dark:text-gray-400`:
     - "Walk through each area at a steady pace"
     - "Narrate what you're showing as you go"
     - "Cover all key work areas and equipment"
     - "Keep the video between 1-5 minutes"

Card wrapper div uses exact CLAUDE.md card anatomy: `rounded-xl border border-gray-200 dark:border-gray-800 bg-white dark:bg-[var(--card-bg)] [box-shadow:var(--card-shadow)] p-5`

Named export: `export { VideoUpload }` and `export type { VideoUploadProps }`
Props interface: `interface VideoUploadProps {}` (empty for now, no props needed)
Add `'use client'` directive.
  </action>
  <verify>
    <automated>cd /Users/arnavsinghal/DashboardSidekick/sidekick-dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>VideoUpload.tsx exists with max-w-2xl constraint on upload zone, purple icon container, drag-and-drop with isDragOver feedback, and tips section. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Build KnowledgeGaps component</name>
  <files>components/dashboard/ai-studio/KnowledgeGaps.tsx</files>
  <action>
Create `components/dashboard/ai-studio/KnowledgeGaps.tsx`.

**Types (at top of file, exported):**
```typescript
interface KnowledgeGap {
  id: string
  question: string
  frequency: number
  category: string
}
```

**Mock data (exported constant):**
```typescript
const MOCK_GAPS: KnowledgeGap[] = [
  { id: '1', question: 'What is the proper procedure for chemical spills?', frequency: 12, category: 'Safety' },
  { id: '2', question: 'Where are the emergency exits on the second floor?', frequency: 8, category: 'Safety' },
  { id: '3', question: 'What are the overtime policies for weekend shifts?', frequency: 6, category: 'HR' },
  { id: '4', question: 'How do I report a broken piece of equipment?', frequency: 5, category: 'Equipment' },
  { id: '5', question: 'What PPE is required in the loading dock area?', frequency: 4, category: 'Safety' },
]
```

**Props:** `interface KnowledgeGapsProps { gaps: KnowledgeGap[] }`

**Structure (inside card wrapper div):**
1. **Header row** (`flex items-start justify-between mb-4`):
   - Left side: amber icon container (48x48, `rounded-xl bg-amber-100 dark:bg-amber-900/30`) with `Zap` icon (`h-6 w-6 text-amber-600 dark:text-amber-400`). Next to it: heading "Generate from Knowledge Gaps" + subtitle "Create policies based on unanswered worker questions".
   - Right side: "Analyze Gaps" button with `className="bg-amber-500 hover:bg-amber-600 text-white border-transparent"`. Do NOT set a `variant` prop -- let className fully override. Include `Zap` icon (h-4 w-4) inside button before text.

2. **Conditional content:**
   - If `gaps.length === 0`: Render `<EmptyState icon={Lightbulb} title="No knowledge gaps detected yet" description="Knowledge gaps will appear here when workers ask questions that your training documents don't cover." />`
   - If gaps exist: Render a list of gap items. Each gap is a `div` with `flex items-center justify-between py-3 border-b border-gray-100 dark:border-gray-800 last:border-0`:
     - Left: question text (`text-sm text-gray-900 dark:text-white`) with category badge below (`text-xs text-gray-500 dark:text-gray-400`)
     - Right: frequency count as `text-sm font-medium text-gray-500 dark:text-gray-400` showing "{frequency} asks"

Card wrapper: exact CLAUDE.md card anatomy class string.
Named exports: `export { KnowledgeGaps, MOCK_GAPS }`, `export type { KnowledgeGapsProps, KnowledgeGap }`
Add `'use client'` directive.
  </action>
  <verify>
    <automated>cd /Users/arnavsinghal/DashboardSidekick/sidekick-dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>KnowledgeGaps.tsx exists with amber Analyze Gaps button, conditional EmptyState/gap-list rendering, and exported MOCK_GAPS data. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds with no TypeScript errors
- Both files exist in `components/dashboard/ai-studio/`
- VideoUpload has `max-w-2xl` class on the upload zone wrapper
- KnowledgeGaps has `bg-amber-500` on the Analyze Gaps button (not blue)
</verification>

<success_criteria>
- VideoUpload renders drag-and-drop zone constrained to max-w-2xl with purple icon + tips
- KnowledgeGaps renders amber CTA + conditional gap list / EmptyState
- Both components use exact CLAUDE.md card anatomy and follow established patterns
- Build compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-studio-tab/08-01-SUMMARY.md`
</output>
