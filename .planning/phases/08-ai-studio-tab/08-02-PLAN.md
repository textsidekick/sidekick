---
phase: 08-ai-studio-tab
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - components/dashboard/ai-studio/AIStudioTab.tsx
  - app/dashboard/page.tsx
autonomous: true
requirements: [AIST-03]

must_haves:
  truths:
    - "AI Studio tab renders VideoUpload and KnowledgeGaps sections in a vertical layout"
    - "showMockData=true populates KnowledgeGaps with mock gap data"
    - "showMockData=false shows EmptyState in KnowledgeGaps"
    - "AI Studio tab has a Create Content page heading with Sparkles icon"
    - "Dashboard page renders AIStudioTab instead of Coming Soon placeholder"
  artifacts:
    - path: "components/dashboard/ai-studio/AIStudioTab.tsx"
      provides: "AI Studio tab assembly component"
      exports: ["AIStudioTab", "AIStudioTabProps"]
    - path: "app/dashboard/page.tsx"
      provides: "Dashboard page with AI Studio tab wired in"
      contains: "AIStudioTab"
  key_links:
    - from: "components/dashboard/ai-studio/AIStudioTab.tsx"
      to: "components/dashboard/ai-studio/VideoUpload.tsx"
      via: "import VideoUpload"
      pattern: "import.*VideoUpload"
    - from: "components/dashboard/ai-studio/AIStudioTab.tsx"
      to: "components/dashboard/ai-studio/KnowledgeGaps.tsx"
      via: "import KnowledgeGaps + MOCK_GAPS"
      pattern: "import.*KnowledgeGaps.*MOCK_GAPS"
    - from: "app/dashboard/page.tsx"
      to: "components/dashboard/ai-studio/AIStudioTab.tsx"
      via: "import AIStudioTab, replace Coming Soon placeholder"
      pattern: "AIStudioTab.*showMockData"
---

<objective>
Assemble the AIStudioTab component and wire it into the dashboard page, replacing the Coming Soon placeholder.

Purpose: Complete the AI Studio tab as the last tab before the final polish phase.
Output: AIStudioTab assembly + dashboard page wiring.
</objective>

<execution_context>
@/Users/arnavsinghal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arnavsinghal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ai-studio-tab/08-01-SUMMARY.md

<interfaces>
<!-- Components from Plan 01 that this plan assembles -->

From components/dashboard/ai-studio/VideoUpload.tsx:
```typescript
interface VideoUploadProps {}
export { VideoUpload }
```

From components/dashboard/ai-studio/KnowledgeGaps.tsx:
```typescript
interface KnowledgeGap {
  id: string
  question: string
  frequency: number
  category: string
}
interface KnowledgeGapsProps {
  gaps: KnowledgeGap[]
}
export { KnowledgeGaps, MOCK_GAPS }
export type { KnowledgeGapsProps, KnowledgeGap }
```

<!-- Dashboard page pattern (from DocumentsTab wiring) -->
From app/dashboard/page.tsx:
```typescript
// Current AI Studio placeholder:
{activeTab === 'ai-studio' && (
  <EmptyState icon={Sparkles} title="AI Studio Coming Soon" ... />
)}
// Replace with: <AIStudioTab showMockData={true} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble AIStudioTab component</name>
  <files>components/dashboard/ai-studio/AIStudioTab.tsx</files>
  <action>
Create `components/dashboard/ai-studio/AIStudioTab.tsx` following the DocumentsTab assembly pattern exactly.

**Props:** `interface AIStudioTabProps { showMockData?: boolean }`

**Structure:**
1. **Page heading** (first element, before sections): This is unique to AI Studio -- other tabs don't have it.
   - Container: `flex items-center gap-3 mb-6`
   - Sparkles icon from lucide-react: `h-6 w-6 text-purple-600 dark:text-purple-400`
   - Title div:
     - "Create Content" as `text-lg font-semibold text-gray-900 dark:text-white`
     - "Generate training materials, policies, and guides from your facility knowledge" as `text-sm text-gray-500 dark:text-gray-400`

2. **Sections wrapper:** `div.space-y-6` containing:
   - `<VideoUpload />`
   - `<KnowledgeGaps gaps={gaps} />` where `gaps = showMockData ? MOCK_GAPS : []`

Imports:
- `VideoUpload` from `./VideoUpload`
- `KnowledgeGaps, MOCK_GAPS` from `./KnowledgeGaps`
- `Sparkles` from `lucide-react`

Named exports: `export { AIStudioTab }`, `export type { AIStudioTabProps }`
Add `'use client'` directive.
  </action>
  <verify>
    <automated>cd /Users/arnavsinghal/DashboardSidekick/sidekick-dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>AIStudioTab.tsx exists, assembles VideoUpload + KnowledgeGaps with showMockData conditional, includes Create Content heading. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AIStudioTab into dashboard page</name>
  <files>app/dashboard/page.tsx</files>
  <action>
Modify `app/dashboard/page.tsx`:

1. Add import at top: `import { AIStudioTab } from '@/components/dashboard/ai-studio/AIStudioTab'`

2. Find the AI Studio Coming Soon placeholder block:
```typescript
{activeTab === 'ai-studio' && (
  <EmptyState
    icon={Sparkles}
    title="AI Studio Coming Soon"
    description="Create training content and analyze knowledge gaps here. This tab is under construction."
  />
)}
```

3. Replace it with:
```typescript
{activeTab === 'ai-studio' && <AIStudioTab showMockData={true} />}
```

4. Remove the `Sparkles` import from lucide-react ONLY if no other component in the file still uses it. Check before removing.
  </action>
  <verify>
    <automated>cd /Users/arnavsinghal/DashboardSidekick/sidekick-dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Dashboard page renders AIStudioTab with showMockData={true} when AI Studio tab is active. No Coming Soon placeholder remains for AI Studio. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds with no TypeScript errors
- AI Studio tab renders Create Content heading + VideoUpload + KnowledgeGaps
- No "Coming Soon" placeholder remains for AI Studio tab
- showMockData={true} populates knowledge gaps with mock data
</verification>

<success_criteria>
- AIStudioTab assembles both sub-components with showMockData conditional
- Dashboard page shows AIStudioTab instead of Coming Soon placeholder
- Build compiles without errors
- All AIST-* requirements addressed across Plans 01 and 02
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-studio-tab/08-02-SUMMARY.md`
</output>
