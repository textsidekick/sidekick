---
phase: 09-polish-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/ai-studio/StorageSidebar.tsx
  - components/dashboard/ai-studio/ContentCards.tsx
  - components/dashboard/ai-studio/AIStudioTab.tsx
  - components/dashboard/ai-studio/VideoUpload.tsx
  - components/dashboard/ai-studio/KnowledgeGaps.tsx
  - components/dashboard/alerts/AlertCharts.tsx
  - components/dashboard/alerts/AlertMetrics.tsx
  - components/dashboard/alerts/AlertsTab.tsx
  - components/dashboard/alerts/AlertsTable.tsx
  - components/dashboard/analytics/AnalyticsTab.tsx
  - components/dashboard/analytics/AnswerSourcesChart.tsx
  - components/dashboard/analytics/CategorySummary.tsx
  - components/dashboard/analytics/FeedCard.tsx
  - components/dashboard/analytics/HealthScoreCard.tsx
  - components/dashboard/analytics/QuestionsChart.tsx
  - components/dashboard/analytics/QuestionsPerHourChart.tsx
  - components/dashboard/analytics/ResolutionChart.tsx
  - components/dashboard/analytics/TopGapsTable.tsx
  - components/dashboard/documents/DocumentsTab.tsx
  - components/dashboard/documents/DocumentsTable.tsx
  - components/dashboard/documents/IntegrationsRow.tsx
  - components/dashboard/documents/UploadZone.tsx
  - components/dashboard/layout/SubHeader.tsx
  - components/dashboard/layout/TabNav.tsx
  - components/dashboard/layout/TopBar.tsx
  - components/dashboard/shared/EmptyState.tsx
  - components/dashboard/shared/MetricCard.tsx
  - components/dashboard/shared/SectionHeader.tsx
  - components/dashboard/workers/QRCodeModal.tsx
  - components/dashboard/workers/RegistrationCard.tsx
  - components/dashboard/workers/WorkersTab.tsx
  - components/dashboard/workers/WorkersTable.tsx
autonomous: true
requirements: [DARK-01, DARK-02, DARK-04, DSN-03]

must_haves:
  truths:
    - "Every text element in every component has both light and dark mode classes (text-gray-* + dark:text-*)"
    - "Every background element uses correct dark mode token (dark:bg-[var(--card-bg)] for cards, dark:border-gray-800 for borders)"
    - "Every card div uses the exact CLAUDE.md card anatomy class string -- no shadcn Card imports"
    - "Every text element matches the CLAUDE.md typography scale exactly (KPI values = text-3xl font-bold, labels = text-xs font-medium uppercase tracking-wide, etc.)"
    - "Every empty state uses the shared EmptyState component -- no bare icon + text patterns anywhere"
    - "StorageSidebar bare 'No recent activity' text replaced with EmptyState component"
    - "Chart colors are visible against both light (#ffffff) and dark (#1a1d27) card backgrounds"
  artifacts:
    - path: "components/dashboard/ai-studio/StorageSidebar.tsx"
      provides: "Fixed EmptyState usage (DSN-03 compliance)"
      contains: "EmptyState"
    - path: "components/dashboard/alerts/AlertCharts.tsx"
      provides: "Chart colors verified/fixed for dark mode"
      contains: "chart"
  key_links:
    - from: "components/dashboard/ai-studio/StorageSidebar.tsx"
      to: "components/dashboard/shared/EmptyState.tsx"
      via: "import { EmptyState }"
      pattern: "import.*EmptyState.*from.*shared/EmptyState"
---

<objective>
Systematic dark mode sweep, typography audit, card anatomy check, and EmptyState compliance across all 32 dashboard component files.

Purpose: Ensure every component renders correctly in both light and dark mode with zero unreadable text, correct CLAUDE.md typography scale, proper card anatomy class strings, and EmptyState used for all empty states (no bare icon + text patterns).

Output: All 32 component files audited and fixed for dark mode, typography, card anatomy (DSN-01), and empty state compliance (DSN-03). Successful `npx next build`.
</objective>

<execution_context>
@/Users/arnavsinghal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arnavsinghal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish-and-verification/09-RESEARCH.md

<interfaces>
<!-- Typography scale from CLAUDE.md -- the exact classes to enforce -->
Card section labels:  text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wide
KPI values:           text-3xl font-bold text-gray-900 dark:text-white leading-none
Section headings:     text-base font-semibold text-gray-900 dark:text-white
Body text:            text-sm text-gray-500 dark:text-gray-400 leading-relaxed
Table cell primary:   text-sm font-medium text-gray-900 dark:text-white
Table cell secondary: text-sm text-gray-500 dark:text-gray-400
Monospace JOIN code:  font-mono text-2xl font-bold text-blue-600 dark:text-blue-400 tracking-wider
Muted helper text:    text-xs text-gray-400 dark:text-gray-500

<!-- Card anatomy class string -- exact string for every card -->
rounded-xl border border-gray-200 dark:border-gray-800 bg-white dark:bg-[var(--card-bg)] [box-shadow:var(--card-shadow)] p-5

<!-- Hover-interactive cards additionally get: -->
hover:[box-shadow:var(--card-shadow-hover)] transition-shadow duration-150 cursor-pointer

<!-- EmptyState component interface -->
From components/dashboard/shared/EmptyState.tsx:
Props: icon (LucideIcon), title (string), description (string), action? (ReactNode)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark mode sweep + EmptyState compliance (DSN-03) across all 32 files</name>
  <files>All 32 files in components/dashboard/ (see files_modified above)</files>
  <action>
Read every component file in components/dashboard/ (all 32 files). For each file, check and fix:

**Dark mode (DARK-01, DARK-02, DARK-04):**
- Every text element has both light and dark classes (e.g., `text-gray-900 dark:text-white` for primary text, `text-gray-500 dark:text-gray-400` for secondary)
- Every background uses correct dark token (`dark:bg-[var(--card-bg)]` for cards, `dark:bg-gray-800` or `dark:bg-gray-900` for containers)
- Every border has dark variant (`dark:border-gray-800` for card borders, `dark:border-gray-700` for internal dividers)
- No hardcoded colors without dark counterparts
- `useTheme()` usage always has mounted guard pattern (useState + useEffect)

**EmptyState compliance (DSN-03):**
- Every empty/zero state MUST use the shared EmptyState component (import from `../shared/EmptyState`)
- Known violation: StorageSidebar.tsx lines 147-150 has bare "No recent activity" `<p>` tag -- replace with `<EmptyState icon={Clock} title="No recent activity" description="Activity from uploads, policy generation, and gap analysis will appear here" />`
- Scan ALL files for any other bare empty state patterns (icon + single line of text without EmptyState wrapper)

**Chart dark mode (dedicated sub-pass):**
- AlertCharts.tsx: Check hardcoded `fill="#3b82f6"` -- verify it is visible on dark (#1a1d27) background. Blue-500 should be fine, but if any chart color is too dark/invisible against #1a1d27, fix it
- All chart components (QuestionsChart, QuestionsPerHourChart, AnswerSourcesChart, ResolutionChart, HealthScoreCard): Verify isDark conditional color handling is complete and covers all fill/stroke colors
- Chart tooltips: Verify they use card-bg and appropriate text colors for dark mode

**Light mode background rule:** The page background is intentionally #f7f5f0 (warm cream), NOT #f9fafb. Do NOT "fix" this. It is already set in globals.css.

Fix silently. Do not add comments explaining the fix. Keep changes minimal -- only fix what is non-compliant.
  </action>
  <verify>
    <automated>cd /Users/arnavsinghal/DashboardSidekick/sidekick-dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>All 32 files pass dark mode audit (every text/bg/border has dark: variant), StorageSidebar EmptyState fixed, no bare empty states remain, chart colors verified for dark backgrounds, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Typography scale audit + card anatomy check (DSN-01) across all 32 files</name>
  <files>All 32 files in components/dashboard/ (see files_modified above)</files>
  <action>
Read every component file again (after dark mode fixes). For each file, check and fix:

**Typography scale enforcement:**
- KPI values (big numbers in MetricCard): Must be `text-3xl font-bold text-gray-900 dark:text-white leading-none`
- Card section labels: Must be `text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wide`
- Section/card headings: Must be `text-base font-semibold text-gray-900 dark:text-white`
- Body/description text: Must be `text-sm text-gray-500 dark:text-gray-400 leading-relaxed`
- Table cell primary: Must be `text-sm font-medium text-gray-900 dark:text-white`
- Table cell secondary: Must be `text-sm text-gray-500 dark:text-gray-400`
- Muted helper text: Must be `text-xs text-gray-400 dark:text-gray-500`
- JOIN code in RegistrationCard: Must be `font-mono text-2xl font-bold text-blue-600 dark:text-blue-400 tracking-wider`

Note: Some text elements are intentionally different (e.g., colored badge text, chart labels). Only enforce the typography scale on elements that SHOULD match. Use judgment for elements unique to specific components (e.g., category labels, chart legends).

**Card anatomy (DSN-01):**
- Every card-like container div must use the exact class string: `rounded-xl border border-gray-200 dark:border-gray-800 bg-white dark:bg-[var(--card-bg)] [box-shadow:var(--card-shadow)] p-5`
- Verify NO shadcn `<Card>` component is imported anywhere in components/dashboard/
- Verify NO `shadow-[var(--card-shadow)]` usage (must be `[box-shadow:var(--card-shadow)]`)
- Cards that are hover-interactive also need: `hover:[box-shadow:var(--card-shadow-hover)] transition-shadow duration-150 cursor-pointer`

Fix any deviations. Keep changes minimal.
  </action>
  <verify>
    <automated>cd /Users/arnavsinghal/DashboardSidekick/sidekick-dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>All text elements match CLAUDE.md typography scale, all cards use exact card anatomy class string, no shadcn Card imports exist, no shadow-[var(...)] usage exists, build succeeds</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx next build` succeeds with zero errors
2. `grep -r "from.*@/components/ui/card" components/dashboard/` returns no results (no shadcn Card usage)
3. `grep -r "shadow-\[var(" components/dashboard/` returns no results (no incorrect shadow syntax)
4. Every file in components/dashboard/ has dark: classes on text and background elements
5. StorageSidebar.tsx imports and uses EmptyState component
</verification>

<success_criteria>
- All 32 component files pass dark mode audit with correct light and dark classes
- Typography scale matches CLAUDE.md exactly across all components
- Card anatomy uses exact CLAUDE.md class string everywhere (DSN-01)
- All empty states use EmptyState component (DSN-03) -- zero bare patterns
- Chart colors visible on both light and dark backgrounds
- `npx next build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-and-verification/09-01-SUMMARY.md`
</output>
