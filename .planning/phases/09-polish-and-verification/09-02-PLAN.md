---
phase: 09-polish-and-verification
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - components/dashboard/layout/TabNav.tsx
  - components/dashboard/alerts/AlertsTable.tsx
  - components/dashboard/documents/DocumentsTable.tsx
  - components/dashboard/workers/WorkersTable.tsx
  - components/dashboard/ai-studio/ContentCards.tsx
  - app/dashboard/page.tsx
  - CLAUDE.md
autonomous: true
requirements: [DARK-01, DARK-02, DARK-04, DSN-03]

must_haves:
  truths:
    - "All tables (Alerts, Documents, Workers) have overflow-x-auto wrappers for tablet-width horizontal scroll"
    - "TabNav has overflow-x-auto so 5 tabs + Invite Workers button scroll horizontally at tablet width"
    - "ContentCards grid collapses responsively (3 cols desktop, 1 col mobile)"
    - "Metric card grids collapse from 4 columns to 2 to 1 at defined breakpoints"
    - "Every tab renders correctly with showMockData={true} (populated data, no missing values)"
    - "Every tab renders correctly with showMockData={false} (EmptyState component shown, no bare text)"
    - "CLAUDE.md Build State table reflects completed status for every component"
  artifacts:
    - path: "components/dashboard/layout/TabNav.tsx"
      provides: "Horizontal scroll overflow handling"
      contains: "overflow-x-auto"
    - path: "components/dashboard/alerts/AlertsTable.tsx"
      provides: "Table overflow wrapper"
      contains: "overflow-x-auto"
    - path: "components/dashboard/documents/DocumentsTable.tsx"
      provides: "Table overflow wrapper"
      contains: "overflow-x-auto"
    - path: "components/dashboard/workers/WorkersTable.tsx"
      provides: "Table overflow wrapper"
      contains: "overflow-x-auto"
    - path: "components/dashboard/ai-studio/ContentCards.tsx"
      provides: "Responsive grid breakpoints"
      contains: "sm:grid-cols"
    - path: "CLAUDE.md"
      provides: "Final Build State table with all components marked complete"
      contains: "Complete"
  key_links:
    - from: "app/dashboard/page.tsx"
      to: "components/dashboard/*/Tab.tsx"
      via: "showMockData prop"
      pattern: "showMockData=\\{true\\}"
---

<objective>
Responsive layout fixes, showMockData validation across all 5 tabs, and final CLAUDE.md Build State update.

Purpose: Ensure the dashboard is fully responsive at desktop/laptop/tablet widths, all tabs work correctly with both populated and empty states, and the project's final status is recorded.

Output: Responsive overflow handling on all tables and TabNav, validated showMockData toggle, updated CLAUDE.md Build State table. Demo-ready dashboard.
</objective>

<execution_context>
@/Users/arnavsinghal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arnavsinghal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish-and-verification/09-RESEARCH.md
@.planning/phases/09-polish-and-verification/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Responsive fixes -- table overflow, TabNav scroll, ContentCards grid</name>
  <files>
    components/dashboard/layout/TabNav.tsx
    components/dashboard/alerts/AlertsTable.tsx
    components/dashboard/documents/DocumentsTable.tsx
    components/dashboard/workers/WorkersTable.tsx
    components/dashboard/ai-studio/ContentCards.tsx
  </files>
  <action>
Apply responsive fixes to prevent layout breakage at tablet width (768px):

**Table overflow (AlertsTable, DocumentsTable, WorkersTable):**
- Wrap each `<Table>` component in a `<div className="overflow-x-auto">` container
- This ensures all table columns remain accessible via horizontal scroll at narrow widths instead of breaking the page layout
- TopGapsTable already has this pattern -- match it

**TabNav overflow:**
- Add `overflow-x-auto` to the tab container div (the flex row containing all 5 tab buttons)
- This prevents tab wrapping at tablet widths where 5 tabs + Invite Workers button may exceed container width

**ContentCards responsive grid:**
- Change `grid grid-cols-3 gap-4` to `grid grid-cols-1 sm:grid-cols-3 gap-4`
- This ensures cards stack on narrow viewports and display in 3 columns on desktop

**Metric card grid verification:**
- Check each tab assembly file (AlertsTab, AnalyticsTab) for metric card grid classes
- Verify they use responsive breakpoints like `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` (or similar 4->2->1 collapse)
- Fix any that use only `grid-cols-4` without responsive breakpoints

Keep changes minimal. Only add the overflow/responsive classes.
  </action>
  <verify>
    <automated>cd /Users/arnavsinghal/DashboardSidekick/sidekick-dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>All 3 tables have overflow-x-auto wrappers, TabNav has overflow-x-auto, ContentCards has responsive grid, metric card grids have responsive breakpoints, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: showMockData validation + CLAUDE.md Build State update</name>
  <files>
    app/dashboard/page.tsx
    CLAUDE.md
  </files>
  <action>
**showMockData validation:**
Read app/dashboard/page.tsx. Verify every tab component is rendered with `showMockData={true}`. Then read each tab assembly file and verify:
1. AlertsTab: showMockData prop controls mock data vs EmptyState display
2. AnalyticsTab: showMockData prop controls mock data vs empty feeds/chart
3. WorkersTab: showMockData prop controls mock workers vs EmptyState
4. DocumentsTab: showMockData prop controls mock documents vs EmptyState
5. AIStudioTab: showMockData prop controls mock knowledge gaps vs EmptyState

For each tab, trace the showMockData prop path to confirm it reaches all sub-components that need it. If any sub-component ignores showMockData and always shows data (or always shows empty), fix the prop threading.

**CLAUDE.md Build State update:**
Read CLAUDE.md, find the Build State table. Update EVERY component row to reflect its actual current status. Use these markers:
- Components that are fully built and audited (which is all of them after this phase): checkmark with "Complete -- audited Phase 9" note
- Be specific in Notes: "Built Phase X, audited Phase 9" for each component

Mark all components as complete since all 5 tabs are built and this phase audits them all.
  </action>
  <verify>
    <automated>cd /Users/arnavsinghal/DashboardSidekick/sidekick-dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 tabs verified with showMockData={true} and showMockData={false} paths, CLAUDE.md Build State table fully updated with complete status for all components, build succeeds</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx next build` succeeds with zero errors
2. `grep -r "overflow-x-auto" components/dashboard/` includes AlertsTable, DocumentsTable, WorkersTable, TabNav
3. ContentCards.tsx contains responsive grid classes (sm:grid-cols)
4. CLAUDE.md Build State table has no "Not started" entries remaining
5. Every tab assembly file threads showMockData to sub-components correctly
</verification>

<success_criteria>
- All tables scroll horizontally at tablet width (overflow-x-auto)
- TabNav scrolls horizontally when tabs exceed container width
- ContentCards grid collapses responsively
- Metric card grids collapse 4->2->1 at breakpoints
- showMockData={true} populates all tabs, showMockData={false} shows EmptyState everywhere
- CLAUDE.md Build State table marks every component as complete
- `npx next build` succeeds -- dashboard is demo-ready
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-and-verification/09-02-SUMMARY.md`
</output>
